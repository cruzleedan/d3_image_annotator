<!-- last triaged: never -->

# Friction Log

Append-only. Log an entry only when something about this project's own
`context/work/` process itself (not the package's code, and not the
package's *public API* — that belongs in a work item like WORK-0001, not
here) caused a wrong turn, confusion, or extra steps. Most sessions
should add nothing here.

## Format

```
YYYY-MM-DD | <area> | <what happened> | <resolved?>
```
